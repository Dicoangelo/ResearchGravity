# ResearchGravity Dependencies
# ============================
#
# Floors reflect versions actually exercised; upper bounds stop the next major
# from landing silently. CI installs unpinned, so before this pass the declared
# floors described a dependency set nobody ran — sentence-transformers was
# declared >=2.2.0 while 5.x was installed, numpy >=1.24.0 while 2.x was
# installed, pytest-asyncio >=0.21.0 while 1.x (different marker semantics) was
# installed. Bump these deliberately, not by drift.

# MCP Server
mcp>=1.25.0,<2.0.0

# API Server
fastapi>=0.128.0,<1.0.0
uvicorn>=0.40.0,<1.0.0

# Storage
aiofiles>=25.1.0,<26.0.0
aiosqlite>=0.22.0,<1.0.0
asyncpg>=0.31.0,<1.0.0

# Vector Storage (V2)
sqlite-vec>=0.1.0,<1.0.0   # Local vector storage
cohere>=5.0.0,<6.0.0       # Embeddings (optional, for quality)

# Context Packs V2
sentence-transformers>=5.0.0,<6.0.0
numpy>=2.0.0,<3.0.0

# REPL (V2)
rich>=14.0.0,<15.0.0       # Terminal UI
prompt-toolkit>=3.0.0,<4.0.0  # REPL framework (optional)
watchdog>=3.0.0,<7.0.0     # File watcher (optional)

# Async runtime — pulled in transitively by fastapi/starlette, but the delegation
# tests mark themselves pytest.mark.anyio and depend on its pytest plugin, so it
# is a direct dependency and is declared as one.
anyio>=4.0.0,<5.0.0

# Testing
pytest>=8.0.0,<10.0.0
pytest-asyncio>=1.0.0,<2.0.0

# Include CPB dependencies
-r cpb/requirements.txt
